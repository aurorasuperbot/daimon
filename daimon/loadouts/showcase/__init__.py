"""Bundled showcase loadouts for V1 alpha legendary mutations (L1-L6).

This is a data-only package — JSON files referenced by the loader in
`daimon.loadouts.loader`. Do not import anything from here directly.
"""
